{
  "name": "mtg-cypress",
  "version": "1.0.0",
  "description": "Cypress presentation",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --headless",
    "cypress:report": "npx mochawesome-merge \"cypress/reports/*.json\" > mochawesome-report.json & npx marge --reportDir cypress/reports mochawesome-report.json",
    "cypress:full": "npm run cypress:run & npm run cypress:report"
  },
  "author": "Lelexxx",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^13.1.0",
    "cypress-audit": "^1.1.0",
    "typescript": "^5.2.2",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
